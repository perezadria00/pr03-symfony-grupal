<form method="post" action="{{ path('app_nurse_c_r_u_d_delete', {'id': nurse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nurse.id) }}">
    <button class="btn">Delete</button>
</form>
