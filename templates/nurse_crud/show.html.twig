{% extends 'base.html.twig' %}

{% block title %}Nurse{% endblock %}

{% block body %}
    <h1>Nurse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nurse.id }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ nurse.user }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ nurse.password }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nurse.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ nurse.surname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nurse_c_r_u_d_index') }}">back to list</a>

    <a href="{{ path('app_nurse_c_r_u_d_edit', {'id': nurse.id}) }}">edit</a>

    {{ include('nurse_crud/_delete_form.html.twig') }}
{% endblock %}
